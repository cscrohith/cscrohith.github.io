<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Portfolio – Technical Lead | .NET & Enterprise Solutions</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />

<style>
/* ================= BASE THEME ================= */
:root, [data-theme="dark"] {
  --bg: #080c10;
  --surface: #0e1420;
  --surface2: #131928;
  --border: rgba(255,255,255,0.07);
  --accent: #00d4ff;
  --accent2: #7b61ff;
  --accent3: #00ffa3;
  --text: #e8edf5;
  --muted: #6b7a94;
  --heading: #f0f4ff;
  --nav-bg: rgba(8,12,16,0.85);
  --tag-bg: rgba(255,255,255,0.05);
  --orb1: rgba(0,212,255,0.07);
  --orb2: rgba(123,97,255,0.08);
  --orb3: rgba(0,255,163,0.05);
}

* { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: 'DM Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.7;
  overflow-x: hidden;
}

/* ================= ORBS ================= */
.orb {
  position: fixed;
  border-radius: 50%;
  filter: blur(120px);
  pointer-events: none;
  z-index: 0;
  transition: transform 0.2s ease-out;
}
.orb-1 { width: 500px; height: 500px; background: var(--orb1); top: -100px; right: -100px; }
.orb-2 { width: 400px; height: 400px; background: var(--orb2); bottom: 20%; left: -100px; }
.orb-3 { width: 300px; height: 300px; background: var(--orb3); top: 50%; right: 10%; }

/* ================= HERO ================= */
#hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding: 8rem 3rem;
  position: relative;
  z-index: 1;
}

.hero-name {
  font-family: 'Syne', sans-serif;
  font-size: clamp(2.6rem, 5vw, 4.5rem);
  font-weight: 800;
  color: var(--heading);
}

.hero-name::after {
  content: "";
  display: block;
  width: 140px;
  height: 4px;
  margin-top: 14px;
  border-radius: 4px;
  background: linear-gradient(90deg, var(--accent), var(--accent2), var(--accent3));
  animation: glow 3s ease-in-out infinite alternate;
}

@keyframes glow {
  from { filter: blur(0px); opacity: 0.6; }
  to { filter: blur(2px); opacity: 1; }
}

/* ================= BUTTON EFFECT ================= */
.btn-prim {
  background: var(--accent);
  color: #000;
  padding: 0.9rem 1.8rem;
  border-radius: 10px;
  display: inline-block;
  text-decoration: none;
  position: relative;
  overflow: hidden;
  font-weight: 700;
}

.btn-prim::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(120deg, transparent, rgba(255,255,255,0.4), transparent);
  transform: translateX(-100%);
  transition: transform 0.6s ease;
}

.btn-prim:hover::after {
  transform: translateX(100%);
}

/* ================= GLASS CARDS ================= */
.skill-card, .proj-card, .about-card {
  background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02));
  backdrop-filter: blur(14px);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 2rem;
  transition: transform 0.3s ease;
}
.skill-card:hover, .proj-card:hover, .about-card:hover {
  transform: translateY(-6px);
}

/* ================= SECTION TITLE FLOAT ================= */
.sec-title {
  position: relative;
  font-family: 'Syne', sans-serif;
  font-size: 2.2rem;
  margin-bottom: 2rem;
}

.sec-title::before {
  content: attr(data-bg);
  position: absolute;
  top: -45px;
  left: 0;
  font-size: 4.5rem;
  font-weight: 800;
  letter-spacing: 0.12em;
  color: rgba(255,255,255,0.04);
  z-index: -1;
  text-transform: uppercase;
}

/* ================= FOOTER ================= */
footer {
  border-top: 1px solid var(--border);
  padding: 2.5rem;
  text-align: center;
  margin-top: 4rem;
}

.foot-brand {
  font-family: 'Syne', sans-serif;
  font-weight: 800;
  font-size: 1.1rem;
}

.foot-note {
  font-size: 0.9rem;
  color: var(--muted);
}

/* ================= PARALLAX ================= */
</style>
</head>

<body>

<div class="orb orb-1"></div>
<div class="orb orb-2"></div>
<div class="orb orb-3"></div>

<section id="hero">
  <div>
    <h1 class="hero-name">Enterprise .NET Architect</h1>
    <p style="margin:1rem 0 2rem; max-width:600px;">
      Technical Lead with 12+ years of experience building scalable enterprise web and desktop solutions using .NET, SQL Server, and cloud-first architectures.
    </p>
    <a href="#projects" class="btn-prim">View My Work</a>
  </div>
</section>

<section class="sw">
  <h2 class="sec-title" data-bg="Skills">Core Skills</h2>
  <div class="skill-card">C#, ASP.NET, SQL Server, Microservices, CI/CD</div>
</section>

<section class="sw">
  <h2 class="sec-title" data-bg="Projects">Projects</h2>
  <div class="proj-card">Enterprise Automation Platform</div>
</section>

<footer>
  <div class="foot-brand">Dev<span style="color:var(--accent)">.</span>Portfolio</div>
  <div class="foot-note">Let’s build scalable systems together · rohith@email.com</div>
</footer>

<script>
document.addEventListener("mousemove", (e) => {
  const x = (e.clientX / window.innerWidth - 0.5) * 20;
  const y = (e.clientY / window.innerHeight - 0.5) * 20;

  document.querySelector(".orb-1").style.transform = `translate(${x}px, ${y}px)`;
  document.querySelector(".orb-2").style.transform = `translate(${-x}px, ${y}px)`;
  document.querySelector(".orb-3").style.transform = `translate(${x}px, ${-y}px)`;
});
</script>

</body>
</html>
